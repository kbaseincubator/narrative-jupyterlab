This directory is for packages that are all installed as Jupyterlab extensions.  

Accessory Narrative code and types can be found in `../narrative-common` or in the separate `narrative-utils` module.
